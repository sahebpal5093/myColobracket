import { HoverTaskDirective } from './hover-task.directive';

describe('HoverTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTaskDirective();
    expect(directive).toBeTruthy();
  });
});
